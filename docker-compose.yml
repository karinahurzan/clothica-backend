services:
  
  db:
    image: postgres:15
    environment:
      POSTGRES_DB: clothica
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: pwdpwd
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    

  web:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    depends_on:
      - db


volumes:
  postgres_data: